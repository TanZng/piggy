<p id="notice"><%= notice %></p>

<div class="mt-5 grid grid-cols-1 grid-rows-2 md:grid-cols-4">
  <div class="row-start-1 md:col-span-2 ">
    <h1 class="page-title">
      Expense categories
    </h1>
  </div>

  <div class="row-start-2 md:row-start-2 md:col-span-2 md:col-start-3 block-inline">

    <div class="flex flex-row justify-center md:justify-end items-center">

      <div class="pl-5">
        <%= link_to new_categories_category_expense_path,
                    class:"a-create-button" do %>
          + New Expense Category
        <% end %>
      </div>

    </div>

  </div>
</div>


<div class="grid gap-4 grid-cols-1 md:grid-cols-2 lg:grid-cols-3 mt-3">
  <% @categories_category_expenses.each do |categories_category_expense| %>
    <%= render(CategoryCard::IndexComponent.new(category:categories_category_expense)) %>
  <% end %>
</div>

<%=render(Pagination::PagyComponent.new(pagy:@pagy)) %>