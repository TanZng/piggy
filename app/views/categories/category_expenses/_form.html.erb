<%= form_with(model: categories_category_expense) do |form| %>
  <% if categories_category_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categories_category_expense.errors.count, "error") %> prohibited this categories_category_expense from being saved:</h2>

      <ul>
        <% categories_category_expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= render(ColorPicker::Component.new(klass: form.object_name,
                                          form: form,
                                          attribute_name: :color,
                                          color: categories_category_expense.color)) %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
