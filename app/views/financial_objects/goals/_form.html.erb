<%= form_with(model: financial_objects_goal) do |form| %>
  <% if financial_objects_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(financial_objects_goal.errors.count, "error") %> prohibited this financial_objects_goal from being saved:</h2>

      <ul>
        <% financial_objects_goal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :currency %>
    <%= form.text_field :currency %>
  </div>

  <div class="field">
    <%= form.label :due_date %>
    <%= form.date_select :due_date %>
  </div>

  <div class="field">
    <%= form.label :wallet_id %>
    <%= form.text_field :wallet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
